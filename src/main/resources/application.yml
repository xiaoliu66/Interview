spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/interview?characterEncoding=utf-8
    username: root
    password: 123456
  #配置项目热部署
  devtools:
    restart:
      enabled: true

#mybatis
#mybatis:
#  #映射配置文件位置
#  mapper-locations: classpath:dao/*.xml
#  #起别名
#  type-aliases-package: com.baidu.entity

mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  #起别名
  type-aliases-package: com.baidu.entity

# 日志级别
logging:
  level:
    com.baidu.mapper: debug

#tomcat配置
server.port: 8080
server.tomcat.uri-encoding: UTF-8


